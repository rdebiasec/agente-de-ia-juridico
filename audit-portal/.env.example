# Copie este archivo a audit-portal/.env (no commitear .env)
# Mismas variables que GitHub Actions para comportamiento idéntico local/prod.

AUDIT_PORTAL_USERNAME=auditor
AUDIT_PORTAL_PASSWORD=cambie-esta-contraseña-min-12-chars
